version: "3.8"
services:
  db:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
  web:
    build: .
    command: bin/dev
    volumes:
      - .:/wakannyai
    ports:
      - "3000:3000"
    shm_size: "2gb"
    depends_on:
      - db
  test:
    build: .
    depends_on: [db]
    volumes:
      - .:/wakannyai
    environment:
      RAILS_ENV: test
    entrypoint: ""                    # ← ENTRYPOINTを無効化
    command: bash -lc "bundle exec rails db:test:prepare && bundle exec rspec"
    shm_size: "2gb"